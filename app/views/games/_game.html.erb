<div id="<%= dom_id game %>">
  <p>
    <strong>Name:</strong>
    <%= game.name %>
  </p>
  <% if session[:user_type] %>
    <%= @game.subs.pluck(:creator_word) %>
  <% else %>
    <%= @game.subs.pluck(:joiner_word) %>
  <% end %>
</div>
